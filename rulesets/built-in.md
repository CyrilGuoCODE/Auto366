# Auto366 内置规则集

此文件夹包含Auto366的内置规则集，程序启动时会自动导入这些规则集。

## 文件结构说明

- 每个子文件夹代表一个独立的规则集
- `ruleset.json` - 规则集的描述信息和配置
- 与文件夹同名的 `.js` 文件 - 主要的规则集脚本
- 其他文件可自由命名，用于支持功能实现

## 开发说明

如需添加新的规则集：
1. 创建新的文件夹
2. 添加 `ruleset.json` 配置文件
3. 添加 `文件夹名.json` 规则集文件
3. 添加辅助实现规则集功能的文件